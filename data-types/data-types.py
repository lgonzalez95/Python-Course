"""
----Data Types---

numeric: int, float, bool, complex
sequence: list, tuple, string, bytes, bytearray => Have indexes
    list is mutable
    tuple is unmutable
    bytes is inmutable
    bytearray is mutable
Set: set, frozenset => Don't have indexes, the values are not ordered
    set is mutable
    frozenset is unmutable
Dictionary: dict

variable sin memmory are inmmutable
When they are reassigned a new value is created in memmory 
and the old value is deleted from the memory by python VM garbage collector
"""
